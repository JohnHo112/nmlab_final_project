{
  "name": "iota-did-verifier",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "cd frontend && pnpm run dev",
    "public_start": "cd frontend && pnpm run public_dev",
    "server": "cd wasm && npm run start",
    "install-packages": "cd frontend && pnpm install && cd .. && cd wasm && npm install && pnpm install",
    "verifier": "cd iota-verifier && bash verifier.sh",
    "kill-verifier": "cd iota-verifier && bash kill_verifier.sh"
  },
  "dependencies": {
    "@iota/identity-wasm": "^1.0.0",
    "@types/express": "^4.17.21",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "nodemon": "^3.0.2"
  },
  "main": "index.js",
  "keywords": [],
  "author": "",
  "description": ""
}
